import { useEffect } from "react";

// This is a test component to verify that form data is being captured correctly
const ContactFormTest = () => {
  useEffect(() => {
    // This would be used for debugging form data
    console.log("Contact form test component mounted");
  }, []);

  return null; // This component doesn't render anything
};

export default ContactFormTest; 